{
  "name": "epub-renderer",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build2": "yarn build --outDir ./build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.7",
    "vite-plugin-singlefile": "0.7.1"
  },
  "dependencies": {
    "search-text-highlight": "^1.0.7",
    "url-join": "^5.0.0"
  }
}
